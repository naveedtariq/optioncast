.row-fluid
  .span12
    .row-fluid
      .span10
        .row-fluid
          .span12
            %section.utopia-widget
              .utopia-widget-title
                = image_tag("img/icons/paragraph_justify.png", class: "utopia-widget-icon")
                %span About Your Financial Status
              .utopia-widget-content
                -@financial_status.each_with_index do |status,index|
                  -if status != ""
                    %p= status
                    -if index >= 2
                      %br